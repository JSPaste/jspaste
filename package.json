{
  "name": "jspaste",
  "version": "0.0.0",
  "description": "Powerful library to easily interact with JSPaste API",
  "license": "MIT",
  "author": "tnfAngel",
  "contributors": [
    "Aidak",
    "Ivan Gabaldon <git[at]inetol.net>"
  ],
  "homepage": "https://jspaste.tnfangel.xyz",
  "repository": "github:jspaste/jspaste",
  "bugs": {
    "url": "https://github.com/jspaste/jspaste/issues"
  },
  "type": "module",
  "module": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run build:check && bun build ./lib/index.ts --outdir ./dist --target node --minify ",
    "build:check": "tsc",
    "prepublishOnly": "bun run test && bun run build",
    "node:test": "npm run build:check && cross-env NODE_OPTIONS=--experimental-vm-modules jest --no-cache",
    "test": "bun run build && bun test --bail=3 --timeout=10000 --todo --coverage"
  },
  "sideEffects": true,
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "cross-env": "^7.0.3",
    "bun-types": "latest",
    "typescript": "^5.2.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  },
  "peerDependencies": {
    "undici": "^5.25.2"
  },
  "peerDependenciesMeta": {
    "undici": {
      "optional": true
    }
  },
  "keywords": [
    "hastebin",
    "jspaste",
    "paste",
    "pastebin",
    "tnfangel"
  ],
  "jest": {
    "clearMocks": true,
    "collectCoverage": true,
    "collectCoverageFrom": [
      "lib/**/*.ts"
    ],
    "coverageReporters": [
      "json"
    ],
    "errorOnDeprecated": true,
    "preset": "ts-jest",
    "roots": [
      "<rootDir>/node_test/"
    ],
    "testTimeout": 1e4
  }
}